ComplexUnit.Tweaks.Vehicles
===========================

**Build**

To work with this project you need to verify if the path to the `Captain of Industry` installation is correctly set in `Directory.Build.props`.
Use the `Publish` button in Visual Studio to compile a release-ready assembly with all necessary files. You'll find the output in `bin\Publish`.